VITE_API_URL=http://localhost:3000/api

# Must match backend's API_TOKEN (.env) - used by the kiosk app to call
# authenticated endpoints like /api/residents
VITE_API_TOKEN=your-secret-token-here-change-this

# Simple PIN to unlock /admin/residents (resident enrollment screen)
VITE_ADMIN_PIN=1234
